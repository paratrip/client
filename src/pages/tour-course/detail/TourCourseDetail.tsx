import { Suspense } from 'react';

import { StaticMap } from 'react-kakao-maps-sdk';

export default function TourCourseDetail() {
  return (
    <Suspense>
      <section>
        {/* <StaticMap
          level={3}
          center={{ lat: 126.734086, lng: 127.269311 }}
          marker={{}}
        ></StaticMap> */}
      </section>
    </Suspense>
  );
}
